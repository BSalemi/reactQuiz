import checkAnswer from './Question'

describe ('Question', () => {

})


describe("checkAnswer function", (event) => {
    test("it should apply a colored border around the selection depending on if it is correct or not", () => {
    })
  });